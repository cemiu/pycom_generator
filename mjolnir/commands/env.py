

def env(args):
    # TODO: add additional information about environment later,
    #       e.g. GPU, RAM, etc. & assumed configuration
    #       (e.g. CUDA, max seq length, max ram)
    # And installation status (e.g. hhblits, metapsicov, plmdca)
    pass
